{% extends 'base.html.twig' %}
{% block title %}Ajouter Statut {% endblock %}

{% block body %}

    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Modifier status</h4>
            {{ form_start(form) }}
            <div class="form-group row">
                <label for="exampleInputUsername2" class="col-sm-3 col-form-label">Nom du status</label>
                <div class="col-sm-9">
                    {{ form_widget(form.libelle) }}
                </div>
            </div>
            {{ form_widget(form.enregistrer) }}
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}